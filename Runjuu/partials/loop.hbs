{{!文章列表}}
{{#foreach posts}}
<article class="{{post_class}}">
        <header class="post-header">
            <a href="{{url}}">
                <h2 class="post-title">{{{title}}}</h2>
            </a>
        </header>
</article>
{{/foreach}}